# Plan features S0 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerrar los 5 agujeros de table-stakes (§0 del brainstorm) antes del lanzamiento: imágenes de portada en proyectos, links sociales + contacto, QR/share del perfil, export + borrado de cuenta (GDPR), y el bug de roles en móvil.

**Architecture:** Next.js 16 App Router + React 19 Server Actions + Supabase (SSR client para usuario, admin client con service role solo para borrado de cuenta). Una migración SQL (`002_s0_features.sql`) añade 3 columnas y el bucket `project-images`. La lógica pura de links vive en `lib/links.ts` con tests vitest; el resto es UI de componentes existentes y server actions que siguen el patrón ya establecido (`uploadAvatar`, `updateProfile`).

**Tech Stack:** Next.js 16.2, React 19, TypeScript, Tailwind v4, Supabase (`@supabase/ssr` + `@supabase/supabase-js`), Framer Motion, vitest. Nueva dependencia: `qrcode`.

**Decisiones de producto (cerradas en brainstorm 10 jun):**
- Links: modelo **curado + custom** → columna `links jsonb` en `profiles`.
- Contacto: **botón "Get in touch" dedicado** → columna `contact_email text` + botón mailto en el hero.
- Cuenta: **export (JSON) Y borrado real** (ambos), borrado con confirmación escribiendo el username + `admin.deleteUser` (service role).
- Imágenes de proyecto: **una sola imagen de portada** por proyecto (galería = post-launch).

**Convenciones del repo a respetar:**
- Server actions en `app/actions/*.ts` con `'use server'`, `createClient` de `@/lib/supabase/server`, comprueban `supabase.auth.getUser()` y devuelven `{ error }` o `{ success: true }`.
- Validación de imágenes: `validateImageUpload(file)` en `lib/utils.ts` (JPEG/PNG/WebP/GIF, máx 5 MB).
- Estética: dark, `liquid-glass`, `font-body`/`font-heading`, acento `violet-400`, `focus-visible:ring-2`. Copiar las clases de inputs existentes literalmente.
- Imágenes remotas: el proyecto NO configura `next/image` remotePatterns; el avatar público usa `<img>` plano con `eslint-disable-next-line @next/next/no-img-element`. **Usar `<img>` para las portadas de proyecto**, no `next/image`, para evitar tocar `next.config.ts`.
- Test runner: `npm test` (= `vitest run`). Tests junto al código: `lib/*.test.ts`.
- `SUPABASE_SERVICE_ROLE_KEY` solo server-side. Nunca prefijo `NEXT_PUBLIC_`.

---

## Prerequisito manual (hacer una vez antes de las tareas que lo usan)

En el **dashboard de Supabase → Storage**, crear un bucket público llamado `project-images`
(igual que `avatars` y `update-images`). Las políticas RLS de Storage se aplican en la Task 2.
Sin este bucket, la subida de imágenes de la Task 6 fallará.

---

## File Structure

**Crear:**
- `supabase/migrations/002_s0_features.sql` — columnas nuevas + políticas del bucket `project-images`
- `lib/links.ts` — helpers puros de links (plataformas, normalización de URL, validación)
- `lib/links.test.ts` — tests vitest de los helpers
- `lib/supabase/admin.ts` — cliente admin (service role) para borrado de cuenta
- `app/actions/account.ts` — server actions `deleteAccount` (export va por route handler)
- `app/api/export/route.ts` — descarga JSON con todos los datos del usuario
- `components/profile/ContactLinks.tsx` — fila de iconos sociales + botón "Get in touch" (perfil público)
- `components/dashboard/ShareProfile.tsx` — copiar link + descargar QR
- `components/dashboard/DeleteAccountSection.tsx` — UI de borrado con confirmación

**Modificar:**
- `lib/types.ts` — añadir `ProfileLink`, y campos a `Profile`/`Project`
- `components/dashboard/ProfileForm.tsx` — fix roles móvil + editor de links + email de contacto
- `app/actions/profile.ts` — parsear/validar `links` y `contact_email` en `updateProfile`
- `components/profile/ProfileHero.tsx` — render de `ContactLinks`
- `app/[username]/page.tsx` — pasar `links`/`contact_email` al hero (ya hace `select('*')`)
- `components/dashboard/ProjectForm.tsx` — campo de imagen de portada
- `app/actions/projects.ts` — `uploadProjectImage` + guardar `cover_image_url` en create/update
- `components/dashboard/ProjectsClient.tsx` — mostrar miniatura en dashboard
- `components/profile/ProjectGrid.tsx` — mostrar portada en perfil público
- `app/dashboard/profile/page.tsx` — montar `ShareProfile`
- `app/dashboard/settings/page.tsx` — sección export + montar `DeleteAccountSection`
- `.env.local.example` — añadir `NEXT_PUBLIC_SITE_URL`
- `package.json` — dependencia `qrcode`

---

## Task 1: Fix del input de Roles en móvil

El bug: en `ProfileForm.tsx` los roles solo se añaden con `Enter` (`onKeyDown`). En teclados táctiles la tecla "Go/Enter" no dispara fiable `key === 'Enter'` y el foco salta al siguiente campo. Falta un botón explícito. Solución: botón "+ Add" + `enterKeyHint="done"` + tope visual de 10 roles (el server ya valida 10).

**Files:**
- Modify: `components/dashboard/ProfileForm.tsx`

- [ ] **Step 1: Reemplazar la función `addRole` por una función `addRole` reutilizable + handler de teclado**

En `components/dashboard/ProfileForm.tsx`, sustituir el bloque actual (líneas ~56-65):

```tsx
  function addRole(e: React.KeyboardEvent<HTMLInputElement>) {
    if (e.key === 'Enter') {
      e.preventDefault()
      const trimmed = roleInput.trim()
      if (trimmed && !roles.includes(trimmed)) {
        setRoles([...roles, trimmed])
      }
      setRoleInput('')
    }
  }
```

por:

```tsx
  const MAX_ROLES = 10

  function commitRole() {
    const trimmed = roleInput.trim()
    if (trimmed && !roles.includes(trimmed) && roles.length < MAX_ROLES) {
      setRoles([...roles, trimmed])
    }
    setRoleInput('')
  }

  function handleRoleKeyDown(e: React.KeyboardEvent<HTMLInputElement>) {
    if (e.key === 'Enter' || e.key === ',') {
      e.preventDefault()
      commitRole()
    }
  }
```

- [ ] **Step 2: Reemplazar el `<input>` de roles por input + botón "+ Add"**

Sustituir el `<input>` de roles actual (el bloque que empieza en `value={roleInput}`, líneas ~213-220) por:

```tsx
          <div className="flex gap-2">
            <input
              type="text"
              value={roleInput}
              onChange={(e) => setRoleInput(e.target.value)}
              onKeyDown={handleRoleKeyDown}
              enterKeyHint="done"
              disabled={roles.length >= MAX_ROLES}
              placeholder={roles.length >= MAX_ROLES ? 'Maximum 10 roles' : 'Add a role'}
              className="flex-1 min-w-0 bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white font-body text-sm placeholder:text-white/20 focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400 focus-visible:ring-offset-1 focus-visible:ring-offset-black focus:border-white/30 transition-colors disabled:opacity-40"
            />
            <button
              type="button"
              onClick={commitRole}
              disabled={!roleInput.trim() || roles.length >= MAX_ROLES}
              className="shrink-0 bg-white/10 hover:bg-white/15 border border-white/[0.12] rounded-xl px-4 py-3 text-sm font-body text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400"
              aria-label="Add role"
            >
              + Add
            </button>
          </div>
```

- [ ] **Step 3: Verificar en desktop y móvil**

Run: `npm run dev`
- Desktop: escribir un rol y pulsar Enter → se añade y el input se limpia. Escribir otro y pulsar "+ Add" → se añade. La coma también añade.
- Móvil (DevTools responsive a 375px, O móvil real): tocar "+ Add" añade el rol sin saltar al campo de descripción. Al llegar a 10 roles el input y el botón se deshabilitan.

Expected: en ningún caso el foco salta al textarea de Narrative al confirmar un rol.

- [ ] **Step 4: Commit**

```bash
git add components/dashboard/ProfileForm.tsx
git commit -m "fix(profile): add explicit Add button for roles, fix mobile keyboard tab-out"
```

---

## Task 2: Migración SQL + tipos (links, contact_email, cover_image_url)

**Files:**
- Create: `supabase/migrations/002_s0_features.sql`
- Modify: `lib/types.ts`

- [ ] **Step 1: Crear la migración**

Crear `supabase/migrations/002_s0_features.sql`:

```sql
-- ── S0 features: links, contact email, project cover image ──────────────────────

ALTER TABLE profiles ADD COLUMN IF NOT EXISTS links jsonb NOT NULL DEFAULT '[]'::jsonb;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS contact_email text;

ALTER TABLE projects ADD COLUMN IF NOT EXISTS cover_image_url text;

-- ── Storage policies for the project-images bucket ──────────────────────────────
-- Run AFTER creating the public bucket "project-images" in the Supabase dashboard.

CREATE POLICY "Project images are publicly accessible"
  ON storage.objects FOR SELECT USING (bucket_id = 'project-images');

CREATE POLICY "Users can upload their own project images"
  ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'project-images' AND auth.uid()::text = (storage.foldername(name))[1]);

CREATE POLICY "Users can update their own project images"
  ON storage.objects FOR UPDATE
  USING (bucket_id = 'project-images' AND auth.uid()::text = (storage.foldername(name))[1]);

CREATE POLICY "Users can delete their own project images"
  ON storage.objects FOR DELETE
  USING (bucket_id = 'project-images' AND auth.uid()::text = (storage.foldername(name))[1]);
```

- [ ] **Step 2: Aplicar la migración en Supabase**

Ejecutar el contenido de `002_s0_features.sql` en el **SQL Editor** del dashboard de Supabase
(o `supabase db push` si usas la CLI). Recuerda: el bucket `project-images` debe existir antes
(ver "Prerequisito manual").

Verificar en Supabase → Table editor que `profiles` tiene `links` (jsonb) y `contact_email` (text),
y que `projects` tiene `cover_image_url` (text).

- [ ] **Step 3: Actualizar `lib/types.ts`**

Reemplazar el contenido de `lib/types.ts` añadiendo `ProfileLink` y los campos nuevos. Sustituir las interfaces `Profile` y `Project` por:

```ts
export type ProfileLinkType =
  | 'github'
  | 'x'
  | 'linkedin'
  | 'instagram'
  | 'youtube'
  | 'website'
  | 'custom'

export interface ProfileLink {
  type: ProfileLinkType
  label: string
  url: string
}

export interface Profile {
  id: string
  username: string
  full_name: string | null
  tagline: string | null
  roles: string[]
  narrative: string | null
  avatar_url: string | null
  links: ProfileLink[]
  contact_email: string | null
  is_public: boolean
  plan: 'free' | 'pro'
  created_at: string
}

export interface Project {
  id: string
  user_id: string
  name: string
  description: string | null
  status: 'in_progress' | 'launched'
  url: string | null
  cover_image_url: string | null
  display_order: number
  created_at: string
}
```

(El resto del archivo —`Update`, `GithubActivity`, `ActivityItem`— no cambia.)

- [ ] **Step 4: Verificar compilación de tipos**

Run: `npx tsc --noEmit`
Expected: puede haber errores en componentes que aún no actualizamos (se arreglan en sus tareas). NO debe haber errores dentro de `lib/types.ts`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/002_s0_features.sql lib/types.ts
git commit -m "feat(db): add links, contact_email, cover_image_url columns + project-images bucket policies"
```

---

## Task 3: Helpers puros de links (`lib/links.ts`) — TDD

**Files:**
- Create: `lib/links.ts`
- Test: `lib/links.test.ts`

- [ ] **Step 1: Escribir el test que falla**

Crear `lib/links.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { normalizeUrl, isValidHttpUrl, validateLinks, PLATFORMS } from './links'
import type { ProfileLink } from './types'

describe('normalizeUrl', () => {
  it('prepends https:// when no scheme present', () => {
    expect(normalizeUrl('github.com/raul')).toBe('https://github.com/raul')
  })
  it('leaves https URLs untouched', () => {
    expect(normalizeUrl('https://x.com/raul')).toBe('https://x.com/raul')
  })
  it('trims surrounding whitespace', () => {
    expect(normalizeUrl('  github.com/raul  ')).toBe('https://github.com/raul')
  })
})

describe('isValidHttpUrl', () => {
  it('accepts http and https', () => {
    expect(isValidHttpUrl('https://x.com')).toBe(true)
    expect(isValidHttpUrl('http://x.com')).toBe(true)
  })
  it('rejects other schemes and garbage', () => {
    expect(isValidHttpUrl('javascript:alert(1)')).toBe(false)
    expect(isValidHttpUrl('not a url')).toBe(false)
  })
})

describe('PLATFORMS', () => {
  it('includes the curated set and custom', () => {
    const types = PLATFORMS.map((p) => p.type)
    expect(types).toContain('github')
    expect(types).toContain('x')
    expect(types).toContain('custom')
  })
})

describe('validateLinks', () => {
  const ok: ProfileLink[] = [{ type: 'github', label: 'GitHub', url: 'https://github.com/raul' }]

  it('returns null for a valid list', () => {
    expect(validateLinks(ok)).toBeNull()
  })
  it('rejects more than 8 links', () => {
    const many = Array.from({ length: 9 }, (): ProfileLink => ({ type: 'website', label: 'Site', url: 'https://a.com' }))
    expect(validateLinks(many)).toMatch(/maximum/i)
  })
  it('rejects an invalid url', () => {
    expect(validateLinks([{ type: 'website', label: 'Site', url: 'ftp://x' }])).toMatch(/url/i)
  })
  it('rejects an empty label', () => {
    expect(validateLinks([{ type: 'custom', label: '', url: 'https://a.com' }])).toMatch(/label/i)
  })
})
```

- [ ] **Step 2: Ejecutar el test para verificar que falla**

Run: `npm test -- links`
Expected: FAIL — "Cannot find module './links'".

- [ ] **Step 3: Implementar `lib/links.ts`**

Crear `lib/links.ts`:

```ts
import type { ProfileLink, ProfileLinkType } from './types'

export interface PlatformDef {
  type: ProfileLinkType
  label: string
  placeholder: string
}

export const PLATFORMS: PlatformDef[] = [
  { type: 'github', label: 'GitHub', placeholder: 'github.com/username' },
  { type: 'x', label: 'X', placeholder: 'x.com/username' },
  { type: 'linkedin', label: 'LinkedIn', placeholder: 'linkedin.com/in/username' },
  { type: 'instagram', label: 'Instagram', placeholder: 'instagram.com/username' },
  { type: 'youtube', label: 'YouTube', placeholder: 'youtube.com/@channel' },
  { type: 'website', label: 'Website', placeholder: 'yoursite.com' },
  { type: 'custom', label: 'Custom', placeholder: 'https://...' },
]

export const MAX_LINKS = 8

export function normalizeUrl(input: string): string {
  const trimmed = input.trim()
  if (!trimmed) return ''
  if (/^https?:\/\//i.test(trimmed)) return trimmed
  return `https://${trimmed}`
}

export function isValidHttpUrl(url: string): boolean {
  try {
    const u = new URL(url)
    return u.protocol === 'http:' || u.protocol === 'https:'
  } catch {
    return false
  }
}

export function validateLinks(links: ProfileLink[]): string | null {
  if (links.length > MAX_LINKS) return `Maximum ${MAX_LINKS} links allowed`
  for (const link of links) {
    if (!link.label || !link.label.trim()) return 'Each link needs a label'
    if (!isValidHttpUrl(link.url)) return `Invalid URL: ${link.url || '(empty)'}`
    if (link.label.length > 40) return 'Link label must be under 40 characters'
  }
  return null
}
```

- [ ] **Step 4: Ejecutar el test para verificar que pasa**

Run: `npm test -- links`
Expected: PASS (todos los `describe` en verde).

- [ ] **Step 5: Commit**

```bash
git add lib/links.ts lib/links.test.ts
git commit -m "feat(links): add pure link helpers (platforms, url normalization, validation) with tests"
```

---

## Task 4: Editor de links + email de contacto en ProfileForm

**Files:**
- Modify: `components/dashboard/ProfileForm.tsx`
- Modify: `app/actions/profile.ts`

- [ ] **Step 1: Ampliar el server action `updateProfile`**

En `app/actions/profile.ts`, añadir el import al principio:

```ts
import { validateLinks, normalizeUrl } from '@/lib/links'
import type { ProfileLink } from '@/lib/types'
```

Dentro de `updateProfile`, después de la línea que calcula `roles` (~línea 23) y antes de las validaciones de longitud, añadir:

```ts
  const contact_email = ((formData.get('contact_email') as string) || '').trim() || null
  if (contact_email && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(contact_email)) {
    return { error: 'Contact email is not valid' }
  }

  const linksRaw = (formData.get('links') as string) || '[]'
  let links: ProfileLink[]
  try {
    links = JSON.parse(linksRaw) as ProfileLink[]
  } catch {
    return { error: 'Links payload is malformed' }
  }
  links = links.map((l) => ({ ...l, url: normalizeUrl(l.url) }))
  const linksError = validateLinks(links)
  if (linksError) return { error: linksError }
```

Y cambiar el objeto `updates` (~línea 29) para incluir los nuevos campos:

```ts
  const updates = {
    full_name,
    tagline,
    roles,
    narrative,
    links,
    contact_email,
  }
```

- [ ] **Step 2: Ampliar la interfaz `Profile` y el estado en `ProfileForm.tsx`**

En `components/dashboard/ProfileForm.tsx`:

1. Añadir el import al principio:

```tsx
import { PLATFORMS, type PlatformDef } from '@/lib/links'
import type { ProfileLink, ProfileLinkType } from '@/lib/types'
```

2. En la `interface Profile` local (líneas ~10-20), añadir dos campos antes de `is_public`:

```tsx
  links: ProfileLink[] | null
  contact_email: string | null
```

3. Junto al resto de `useState` (después de `roleInput`, ~línea 46), añadir:

```tsx
  const [links, setLinks] = useState<ProfileLink[]>(profile?.links ?? [])
  const [linkType, setLinkType] = useState<ProfileLinkType>('github')
  const [linkLabel, setLinkLabel] = useState('')
  const [linkUrl, setLinkUrl] = useState('')
  const [contactEmail, setContactEmail] = useState(profile?.contact_email ?? '')

  const MAX_LINKS_UI = 8

  function addLink() {
    const url = linkUrl.trim()
    if (!url || links.length >= MAX_LINKS_UI) return
    const def = PLATFORMS.find((p) => p.type === linkType) as PlatformDef
    const label = linkLabel.trim() || def.label
    setLinks([...links, { type: linkType, label, url }])
    setLinkLabel('')
    setLinkUrl('')
  }

  function removeLink(index: number) {
    setLinks(links.filter((_, i) => i !== index))
  }
```

- [ ] **Step 3: Añadir los hidden inputs**

Dentro del `<form>`, junto a `<input type="hidden" name="roles" ... />` (~línea 147), añadir:

```tsx
        <input type="hidden" name="links" value={JSON.stringify(links)} />
```

- [ ] **Step 4: Añadir la UI del editor de links + email**

Insertar este bloque dentro del `<form>`, justo **después** del bloque de Narrative (después del `</div>` que cierra Narrative, ~línea 246) y antes del bloque `{state?.error && ...}`:

```tsx
        {/* Contact email */}
        <div className="flex flex-col gap-1.5">
          <label className="font-body text-sm text-white/70 uppercase tracking-wider">
            Contact email
          </label>
          <input
            name="contact_email"
            type="email"
            value={contactEmail}
            onChange={(e) => setContactEmail(e.target.value)}
            placeholder="you@example.com — shown as a “Get in touch” button"
            className="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white font-body text-sm placeholder:text-white/20 focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400 focus-visible:ring-offset-1 focus-visible:ring-offset-black focus:border-white/30 transition-colors"
          />
        </div>

        {/* Links */}
        <div className="flex flex-col gap-2">
          <label className="font-body text-sm text-white/70 uppercase tracking-wider">
            Links
          </label>

          {links.length > 0 && (
            <div className="flex flex-col gap-2 mb-2">
              {links.map((link, i) => (
                <div
                  key={`${link.type}-${i}`}
                  className="liquid-glass rounded-xl px-4 py-2.5 flex items-center justify-between gap-3"
                >
                  <div className="min-w-0">
                    <span className="text-sm font-body text-white/85">{link.label}</span>
                    <span className="block text-xs font-body text-white/40 truncate">{link.url}</span>
                  </div>
                  <button
                    type="button"
                    onClick={() => removeLink(i)}
                    className="text-white/45 hover:text-white/85 transition-colors shrink-0"
                    aria-label={`Remove ${link.label}`}
                  >
                    ×
                  </button>
                </div>
              ))}
            </div>
          )}

          {links.length < MAX_LINKS_UI && (
            <div className="flex flex-col sm:flex-row gap-2">
              <select
                value={linkType}
                onChange={(e) => setLinkType(e.target.value as ProfileLinkType)}
                className="bg-white/5 border border-white/10 rounded-xl px-3 py-3 text-white font-body text-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400 focus:border-white/30 transition-colors"
              >
                {PLATFORMS.map((p) => (
                  <option key={p.type} value={p.type}>{p.label}</option>
                ))}
              </select>
              <input
                type="text"
                value={linkUrl}
                onChange={(e) => setLinkUrl(e.target.value)}
                onKeyDown={(e) => { if (e.key === 'Enter') { e.preventDefault(); addLink() } }}
                placeholder={PLATFORMS.find((p) => p.type === linkType)?.placeholder ?? 'https://...'}
                className="flex-1 min-w-0 bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white font-body text-sm placeholder:text-white/20 focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400 focus:border-white/30 transition-colors"
              />
              {linkType === 'custom' && (
                <input
                  type="text"
                  value={linkLabel}
                  onChange={(e) => setLinkLabel(e.target.value)}
                  placeholder="Label"
                  className="sm:w-32 bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white font-body text-sm placeholder:text-white/20 focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400 focus:border-white/30 transition-colors"
                />
              )}
              <button
                type="button"
                onClick={addLink}
                disabled={!linkUrl.trim()}
                className="shrink-0 bg-white/10 hover:bg-white/15 border border-white/[0.12] rounded-xl px-4 py-3 text-sm font-body text-white transition-colors disabled:opacity-40 disabled:cursor-not-allowed focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400"
              >
                + Add
              </button>
            </div>
          )}
        </div>
```

- [ ] **Step 5: Verificar**

Run: `npm run dev` → ir a `/dashboard/profile`.
- Añadir un link de GitHub (escribir `github.com/tuusuario`, "+ Add"): aparece en la lista.
- Añadir un link "Custom": aparece el campo Label.
- Escribir un contact email, guardar con "Save changes" → recargar la página: links y email persisten.
- Guardar con un email inválido (`abc`) → muestra error "Contact email is not valid".

Run: `npm test -- links` → sigue en PASS.

- [ ] **Step 6: Commit**

```bash
git add components/dashboard/ProfileForm.tsx app/actions/profile.ts
git commit -m "feat(profile): add social links editor and contact email field"
```

---

## Task 5: Render de links + "Get in touch" en el perfil público

**Files:**
- Create: `components/profile/ContactLinks.tsx`
- Modify: `components/profile/ProfileHero.tsx`

- [ ] **Step 1: Crear `ContactLinks.tsx` con iconos inline**

Crear `components/profile/ContactLinks.tsx`:

```tsx
'use client'

import { motion } from 'framer-motion'
import type { ProfileLink, ProfileLinkType } from '@/lib/types'

function LinkIcon({ type }: { type: ProfileLinkType }) {
  const common = { width: 18, height: 18, viewBox: '0 0 24 24', 'aria-hidden': true }
  switch (type) {
    case 'github':
      return (
        <svg {...common} fill="currentColor">
          <path d="M12 .5C5.7.5.5 5.7.5 12c0 5.1 3.3 9.4 7.9 10.9.6.1.8-.3.8-.6v-2c-3.2.7-3.9-1.5-3.9-1.5-.5-1.3-1.3-1.7-1.3-1.7-1.1-.7.1-.7.1-.7 1.2.1 1.8 1.2 1.8 1.2 1 1.8 2.7 1.3 3.4 1 .1-.8.4-1.3.7-1.6-2.6-.3-5.3-1.3-5.3-5.7 0-1.3.5-2.3 1.2-3.1-.1-.3-.5-1.5.1-3.1 0 0 1-.3 3.3 1.2a11.5 11.5 0 0 1 6 0C17.3 4.7 18.3 5 18.3 5c.6 1.6.2 2.8.1 3.1.8.8 1.2 1.8 1.2 3.1 0 4.4-2.7 5.4-5.3 5.7.4.4.8 1.1.8 2.2v3.3c0 .3.2.7.8.6 4.6-1.5 7.9-5.8 7.9-10.9C23.5 5.7 18.3.5 12 .5z" />
        </svg>
      )
    case 'x':
      return (
        <svg {...common} fill="currentColor">
          <path d="M18.2 2H21l-6.5 7.4L22 22h-6.2l-4.9-6.4L5.3 22H2.5l7-8L2 2h6.3l4.4 5.8L18.2 2zm-1.1 18h1.7L7 3.8H5.2L17.1 20z" />
        </svg>
      )
    case 'linkedin':
      return (
        <svg {...common} fill="currentColor">
          <path d="M4.98 3.5A2.5 2.5 0 1 1 0 3.5a2.5 2.5 0 0 1 4.98 0zM.2 8.2h4.6V24H.2V8.2zm7.5 0h4.4v2.2h.1c.6-1.1 2.1-2.3 4.3-2.3 4.6 0 5.5 3 5.5 7V24h-4.6v-6.2c0-1.5 0-3.4-2.1-3.4s-2.4 1.6-2.4 3.3V24H7.7V8.2z" />
        </svg>
      )
    case 'instagram':
      return (
        <svg {...common} fill="none" stroke="currentColor" strokeWidth="1.8">
          <rect x="3" y="3" width="18" height="18" rx="5" />
          <circle cx="12" cy="12" r="4" />
          <circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none" />
        </svg>
      )
    case 'youtube':
      return (
        <svg {...common} fill="currentColor">
          <path d="M23 7.5a3 3 0 0 0-2.1-2.1C19 4.9 12 4.9 12 4.9s-7 0-8.9.5A3 3 0 0 0 1 7.5 31 31 0 0 0 .5 12 31 31 0 0 0 1 16.5a3 3 0 0 0 2.1 2.1c1.9.5 8.9.5 8.9.5s7 0 8.9-.5a3 3 0 0 0 2.1-2.1A31 31 0 0 0 23.5 12 31 31 0 0 0 23 7.5zM9.8 15.3V8.7l5.7 3.3-5.7 3.3z" />
        </svg>
      )
    default:
      return (
        <svg {...common} fill="none" stroke="currentColor" strokeWidth="1.8">
          <circle cx="12" cy="12" r="9" />
          <path d="M3 12h18M12 3a15 15 0 0 1 0 18M12 3a15 15 0 0 0 0 18" />
        </svg>
      )
  }
}

interface ContactLinksProps {
  links: ProfileLink[]
  contactEmail: string | null
}

export default function ContactLinks({ links, contactEmail }: ContactLinksProps) {
  if (links.length === 0 && !contactEmail) return null

  return (
    <motion.div
      initial={{ filter: 'blur(10px)', opacity: 0, y: 20 }}
      animate={{ filter: 'blur(0px)', opacity: 1, y: 0 }}
      transition={{ duration: 0.6, ease: 'easeOut', delay: 0.8 }}
      className="flex flex-col items-center gap-5 mt-6"
    >
      {contactEmail && (
        <a
          href={`mailto:${contactEmail}`}
          className="bg-white text-black font-body text-sm font-medium rounded-full px-6 py-2.5 hover:bg-white/90 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400 focus-visible:ring-offset-2 focus-visible:ring-offset-black"
        >
          Get in touch
        </a>
      )}

      {links.length > 0 && (
        <div className="flex flex-wrap justify-center gap-3">
          {links.map((link, i) => (
            <a
              key={`${link.type}-${i}`}
              href={link.url}
              target="_blank"
              rel="noopener noreferrer"
              className="liquid-glass rounded-full w-10 h-10 flex items-center justify-center text-white/70 hover:text-white transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400"
              aria-label={link.label}
              title={link.label}
            >
              <LinkIcon type={link.type} />
            </a>
          ))}
        </div>
      )}
    </motion.div>
  )
}
```

- [ ] **Step 2: Montar `ContactLinks` en el hero**

En `components/profile/ProfileHero.tsx`:

1. Añadir el import al principio:

```tsx
import ContactLinks from './ContactLinks'
import type { ProfileLink } from '@/lib/types'
```

2. Ampliar la interfaz `ProfileHeroProps.profile` (líneas ~7-12) añadiendo:

```tsx
    links: ProfileLink[] | null
    contact_email: string | null
```

3. Justo después del bloque de Roles (después de su `</motion.div>`, ~línea 98) y dentro del `<div className="relative z-10 ...">`, añadir:

```tsx
        <ContactLinks links={profile.links ?? []} contactEmail={profile.contact_email} />
```

- [ ] **Step 3: Verificar**

`app/[username]/page.tsx` ya hace `select('*')` y pasa `profile` entero al hero, así que `links` y `contact_email` llegan sin cambios adicionales.

Run: `npm run dev` → publicar tu perfil (toggle "Make public") y visitar `/<tu-username>`.
- Si hay contact email → aparece el botón "Get in touch" (al pulsarlo abre el cliente de correo).
- Los links aparecen como iconos circulares; cada uno abre en pestaña nueva.
- Un perfil sin links ni email no muestra nada extra (no hueco vacío).

- [ ] **Step 4: Commit**

```bash
git add components/profile/ContactLinks.tsx components/profile/ProfileHero.tsx
git commit -m "feat(profile): render social links and Get in touch button on public profile"
```

---

## Task 6: Imagen de portada en proyectos

**Files:**
- Modify: `app/actions/projects.ts`
- Modify: `components/dashboard/ProjectForm.tsx`
- Modify: `components/dashboard/ProjectsClient.tsx`
- Modify: `components/profile/ProjectGrid.tsx`

- [ ] **Step 1: Añadir `uploadProjectImage` y guardar `cover_image_url`**

En `app/actions/projects.ts`, añadir el import arriba:

```ts
import { validateImageUpload } from '@/lib/utils'
```

Añadir esta nueva action al final del archivo:

```ts
export async function uploadProjectImage(formData: FormData) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Not authenticated' }

  const file = formData.get('image') as File
  if (!file || file.size === 0) return { error: 'No file provided' }

  const fileError = validateImageUpload(file)
  if (fileError) return { error: fileError }

  const ext = file.name.split('.').pop()
  const path = `${user.id}/${crypto.randomUUID()}.${ext}`

  const { error: uploadError } = await supabase.storage
    .from('project-images')
    .upload(path, file, { upsert: false })

  if (uploadError) return { error: uploadError.message }

  const { data: { publicUrl } } = supabase.storage.from('project-images').getPublicUrl(path)
  return { success: true, url: publicUrl }
}
```

En `createProject`, dentro del objeto `.insert({...})` (~línea 29), añadir el campo:

```ts
    cover_image_url: (formData.get('cover_image_url') as string) || null,
```

En `updateProject`, dentro del objeto `.update({...})` (~línea 59), añadir el campo:

```ts
      cover_image_url: (formData.get('cover_image_url') as string) || null,
```

- [ ] **Step 2: Añadir el campo de imagen en `ProjectForm.tsx`**

En `components/dashboard/ProjectForm.tsx`:

1. Cambiar los imports de React y actions (líneas ~3-4) por:

```tsx
import { useTransition, useState, useRef } from 'react'
import { createProject, updateProject, uploadProjectImage } from '@/app/actions/projects'
```

2. Añadir `cover_image_url` a la interfaz `Project` local (líneas ~7-13):

```tsx
  cover_image_url: string | null
```

3. Dentro del componente, junto a los `useState` existentes (~línea 21), añadir:

```tsx
  const [coverUrl, setCoverUrl] = useState(project?.cover_image_url ?? '')
  const [coverPending, setCoverPending] = useState(false)
  const coverInputRef = useRef<HTMLInputElement>(null)

  async function handleCoverChange(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0]
    if (!file) return
    setError(null)
    setCoverPending(true)
    const fd = new FormData()
    fd.append('image', file)
    const result = await uploadProjectImage(fd)
    setCoverPending(false)
    if (result?.error) setError(result.error)
    else if (result?.url) setCoverUrl(result.url)
  }
```

4. Añadir el hidden input + UI de portada. Justo después de la apertura del `<form action={handleSubmit} ...>` (antes del bloque "Name"), insertar:

```tsx
      <input type="hidden" name="cover_image_url" value={coverUrl} />

      <div>
        <label className="block text-sm font-body text-white/70 tracking-widest uppercase mb-2">
          Cover image
        </label>
        <div className="flex items-center gap-4">
          <div className="relative w-24 h-16 rounded-[0.6rem] overflow-hidden bg-white/[0.06] border border-white/[0.1] flex items-center justify-center shrink-0">
            {coverUrl ? (
              // eslint-disable-next-line @next/next/no-img-element
              <img src={coverUrl} alt="Cover" className="w-full h-full object-cover" />
            ) : (
              <span className="text-white/30 text-xs font-body">No image</span>
            )}
            {coverPending && (
              <div className="absolute inset-0 bg-black/60 flex items-center justify-center">
                <span className="text-white text-xs font-body">…</span>
              </div>
            )}
          </div>
          <div className="flex flex-col gap-1">
            <button
              type="button"
              onClick={() => coverInputRef.current?.click()}
              className="font-body text-sm text-white/75 hover:text-white transition-colors text-left"
            >
              {coverUrl ? 'Replace image' : 'Upload image'}
            </button>
            {coverUrl && (
              <button
                type="button"
                onClick={() => setCoverUrl('')}
                className="font-body text-xs text-white/40 hover:text-white/70 transition-colors text-left"
              >
                Remove
              </button>
            )}
            <p className="font-body text-xs text-white/40">JPG, PNG, WebP or GIF. Max 5 MB.</p>
          </div>
        </div>
        <input
          ref={coverInputRef}
          type="file"
          accept="image/jpeg,image/png,image/webp,image/gif"
          className="hidden"
          onChange={handleCoverChange}
        />
      </div>
```

- [ ] **Step 3: Mostrar la miniatura en el dashboard (`ProjectsClient.tsx`)**

En `components/dashboard/ProjectsClient.tsx`:

1. Añadir `cover_image_url` a la interfaz `Project` local (líneas ~10-17):

```tsx
  cover_image_url: string | null
```

2. Dentro de la tarjeta de proyecto, justo después de la apertura del `<div className="flex items-start justify-between gap-4">` (~línea 119), insertar la miniatura:

```tsx
                {project.cover_image_url && (
                  // eslint-disable-next-line @next/next/no-img-element
                  <img
                    src={project.cover_image_url}
                    alt=""
                    className="w-20 h-14 rounded-[0.6rem] object-cover border border-white/[0.08] shrink-0"
                  />
                )}
```

- [ ] **Step 4: Mostrar la portada en el perfil público (`ProjectGrid.tsx`)**

En `components/profile/ProjectGrid.tsx`, dentro del `<motion.div>` de los proyectos visibles, justo después de su apertura (antes del `<div>` que contiene el status, ~línea 43), insertar:

```tsx
                {project.cover_image_url && (
                  // eslint-disable-next-line @next/next/no-img-element
                  <img
                    src={project.cover_image_url}
                    alt={project.name}
                    className="w-full h-40 object-cover rounded-[0.9rem] mb-5"
                  />
                )}
```

- [ ] **Step 5: Verificar**

Run: `npm run dev` → `/dashboard/projects`.
- Crear un proyecto, subir una imagen → se ve la previsualización; guardar → la miniatura aparece en la lista.
- Editar el proyecto → "Remove" quita la imagen; guardar → desaparece.
- Publicar el perfil y visitar `/<username>` → la portada se muestra encima de la tarjeta del proyecto.
- Subir un archivo >5 MB → muestra "Image must be under 5 MB".

Run: `npx tsc --noEmit` → sin errores en los archivos tocados.

- [ ] **Step 6: Commit**

```bash
git add app/actions/projects.ts components/dashboard/ProjectForm.tsx components/dashboard/ProjectsClient.tsx components/profile/ProjectGrid.tsx
git commit -m "feat(projects): add cover image upload and display on dashboard and public profile"
```

---

## Task 7: Share + QR del perfil

**Files:**
- Modify: `package.json` (dependencia `qrcode`)
- Create: `components/dashboard/ShareProfile.tsx`
- Modify: `app/dashboard/profile/page.tsx`
- Modify: `.env.local.example`

- [ ] **Step 1: Instalar `qrcode`**

Run:
```bash
npm install qrcode && npm install -D @types/qrcode
```
Expected: `qrcode` aparece en `dependencies` y `@types/qrcode` en `devDependencies` de `package.json`.

- [ ] **Step 2: Añadir `NEXT_PUBLIC_SITE_URL` al ejemplo de entorno**

En `.env.local.example`, añadir bajo la sección Supabase:

```
# ── App ──────────────────────────────────────────────────────────────────────
# Public base URL of the deployed app (no trailing slash). Used for share links + QR.
NEXT_PUBLIC_SITE_URL=https://ingegno.app
```

(En local, `.env.local` puede usar `http://localhost:3000`. El componente cae a `window.location.origin` si la variable no está.)

- [ ] **Step 3: Crear `ShareProfile.tsx`**

Crear `components/dashboard/ShareProfile.tsx`:

```tsx
'use client'

import { useEffect, useState } from 'react'
import QRCode from 'qrcode'

interface ShareProfileProps {
  username: string
}

export default function ShareProfile({ username }: ShareProfileProps) {
  const [origin, setOrigin] = useState('')
  const [qrDataUrl, setQrDataUrl] = useState('')
  const [copied, setCopied] = useState(false)

  useEffect(() => {
    const base = process.env.NEXT_PUBLIC_SITE_URL || window.location.origin
    const url = `${base.replace(/\/$/, '')}/${username}`
    setOrigin(url)
    QRCode.toDataURL(url, { margin: 1, width: 480, color: { dark: '#000000', light: '#ffffff' } })
      .then(setQrDataUrl)
      .catch(() => setQrDataUrl(''))
  }, [username])

  async function copyLink() {
    await navigator.clipboard.writeText(origin)
    setCopied(true)
    setTimeout(() => setCopied(false), 1500)
  }

  function downloadQr() {
    if (!qrDataUrl) return
    const a = document.createElement('a')
    a.href = qrDataUrl
    a.download = `ingegno-${username}-qr.png`
    a.click()
  }

  return (
    <div className="liquid-glass rounded-[1.25rem] p-6 flex items-center gap-5">
      {qrDataUrl && (
        // eslint-disable-next-line @next/next/no-img-element
        <img src={qrDataUrl} alt="Profile QR code" className="w-20 h-20 rounded-lg bg-white p-1 shrink-0" />
      )}
      <div className="flex flex-col gap-2 min-w-0">
        <span className="font-body text-sm text-white/85 truncate">{origin}</span>
        <div className="flex gap-2">
          <button
            type="button"
            onClick={copyLink}
            className="bg-white/10 hover:bg-white/15 border border-white/[0.12] rounded-full px-4 py-1.5 text-xs font-body text-white transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400"
          >
            {copied ? 'Copied ✓' : 'Copy link'}
          </button>
          <button
            type="button"
            onClick={downloadQr}
            disabled={!qrDataUrl}
            className="bg-white/10 hover:bg-white/15 border border-white/[0.12] rounded-full px-4 py-1.5 text-xs font-body text-white transition-colors disabled:opacity-40 focus:outline-none focus-visible:ring-2 focus-visible:ring-violet-400"
          >
            Download QR
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Montar `ShareProfile` en la página de perfil**

En `app/dashboard/profile/page.tsx`:

1. Añadir el import:

```tsx
import ShareProfile from '@/components/dashboard/ShareProfile'
```

2. Justo antes de `<ProfileForm profile={profile} />` (~línea 32), añadir (solo si hay username):

```tsx
      {profile?.username && (
        <div className="mb-8">
          <ShareProfile username={profile.username} />
        </div>
      )}
```

- [ ] **Step 5: Verificar**

Run: `npm run dev` → `/dashboard/profile`.
- Aparece el QR + la URL del perfil + botones.
- "Copy link" copia la URL (probar pegando). Cambia a "Copied ✓" un instante.
- "Download QR" descarga `ingegno-<username>-qr.png`. Escanear el PNG con el móvil → abre la URL del perfil.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json components/dashboard/ShareProfile.tsx app/dashboard/profile/page.tsx .env.local.example
git commit -m "feat(profile): add share link + downloadable QR code"
```

---

## Task 8: Cuenta — export de datos + borrado de cuenta

**Files:**
- Create: `lib/supabase/admin.ts`
- Create: `app/api/export/route.ts`
- Create: `app/actions/account.ts`
- Create: `components/dashboard/DeleteAccountSection.tsx`
- Modify: `app/dashboard/settings/page.tsx`

- [ ] **Step 1: Crear el cliente admin (service role)**

Crear `lib/supabase/admin.ts`:

```ts
import { createClient } from '@supabase/supabase-js'

/**
 * Admin client using the service role key. SERVER-ONLY.
 * Never import this into a client component or expose the key.
 */
export function createAdminClient() {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL
  const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY
  if (!url || !serviceKey) {
    throw new Error('Missing Supabase admin env vars')
  }
  return createClient(url, serviceKey, {
    auth: { autoRefreshToken: false, persistSession: false },
  })
}
```

- [ ] **Step 2: Crear el route handler de export**

Crear `app/api/export/route.ts`:

```ts
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Not authenticated' }, { status: 401 })

  const [{ data: profile }, { data: projects }, { data: updates }, { data: githubActivity }] =
    await Promise.all([
      supabase.from('profiles').select('*').eq('id', user.id).single(),
      supabase.from('projects').select('*').eq('user_id', user.id).order('display_order'),
      supabase.from('updates').select('*').eq('user_id', user.id).order('created_at'),
      supabase.from('github_activity').select('*').eq('user_id', user.id).order('committed_at'),
    ])

  const payload = {
    exported_at: new Date().toISOString(),
    account: { id: user.id, email: user.email },
    profile,
    projects: projects ?? [],
    updates: updates ?? [],
    github_activity: githubActivity ?? [],
  }

  return new NextResponse(JSON.stringify(payload, null, 2), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Content-Disposition': 'attachment; filename="ingegno-export.json"',
    },
  })
}
```

Nota: el export NO incluye `github_connections` porque contiene el `access_token` (nunca se expone).

- [ ] **Step 3: Crear la action de borrado de cuenta**

Crear `app/actions/account.ts`:

```ts
'use server'

import { createClient } from '@/lib/supabase/server'
import { createAdminClient } from '@/lib/supabase/admin'
import { redirect } from 'next/navigation'

export type DeleteAccountState = { error?: string }

export async function deleteAccount(
  _prevState: DeleteAccountState,
  formData: FormData
): Promise<DeleteAccountState> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Not authenticated' }

  const { data: profile } = await supabase
    .from('profiles')
    .select('username')
    .eq('id', user.id)
    .single()

  const confirmation = ((formData.get('confirmation') as string) || '').trim()
  if (!profile || confirmation !== profile.username) {
    return { error: 'The username you typed does not match.' }
  }

  const admin = createAdminClient()
  const { error } = await admin.auth.admin.deleteUser(user.id)
  if (error) return { error: error.message }

  await supabase.auth.signOut()
  redirect('/')
}
```

Nota: borrar el usuario de `auth.users` hace cascade a `profiles` y a todo lo demás (FK `ON DELETE CASCADE` en la migración inicial).

- [ ] **Step 4: Crear `DeleteAccountSection.tsx`**

Crear `components/dashboard/DeleteAccountSection.tsx`:

```tsx
'use client'

import { useActionState, useState } from 'react'
import { deleteAccount, type DeleteAccountState } from '@/app/actions/account'

const initialState: DeleteAccountState = {}

export default function DeleteAccountSection({ username }: { username: string }) {
  const [open, setOpen] = useState(false)
  const [state, formAction] = useActionState(deleteAccount, initialState)

  if (!open) {
    return (
      <button
        type="button"
        onClick={() => setOpen(true)}
        className="text-sm font-body text-red-400/70 hover:text-red-400 transition-colors"
      >
        Delete account
      </button>
    )
  }

  return (
    <form action={formAction} className="space-y-3">
      <p className="text-sm font-body text-white/75">
        This permanently deletes your account, profile and all projects. This cannot be undone.
        Type <span className="text-white font-medium">{username}</span> to confirm.
      </p>
      <input
        name="confirmation"
        type="text"
        autoComplete="off"
        placeholder={username}
        className="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white font-body text-sm placeholder:text-white/20 focus:outline-none focus-visible:ring-2 focus-visible:ring-red-400 focus:border-white/30 transition-colors"
      />
      {state.error && <p className="text-xs font-body text-red-400/80">{state.error}</p>}
      <div className="flex gap-3">
        <button
          type="submit"
          className="bg-red-500/80 hover:bg-red-500 text-white font-body text-sm rounded-full px-5 py-2 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-red-400"
        >
          Permanently delete
        </button>
        <button
          type="button"
          onClick={() => setOpen(false)}
          className="text-sm font-body text-white/55 hover:text-white/85 transition-colors"
        >
          Cancel
        </button>
      </div>
    </form>
  )
}
```

- [ ] **Step 5: Añadir export + borrado a Settings**

En `app/dashboard/settings/page.tsx`:

1. Añadir el import:

```tsx
import DeleteAccountSection from '@/components/dashboard/DeleteAccountSection'
```

2. Cambiar el `select` del perfil (~línea 19-23) para incluir el username (ya selecciona `plan, username`, así que sirve). Verificar que es `.select('plan, username')`.

3. Dentro de la sección Account, **reemplazar** el bloque del borde superior con "Sign out" (`<div className="border-t border-white/[0.08] pt-4">...</div>`, líneas ~65-74) por:

```tsx
          <div className="border-t border-white/[0.08] pt-4 flex flex-col gap-4">
            <div className="flex items-center justify-between">
              <span className="text-sm font-body text-white/75">Export your data</span>
              <a
                href="/api/export"
                className="liquid-glass rounded-full px-4 py-1.5 text-xs font-body text-white/70 hover:text-white transition-colors"
              >
                Download JSON
              </a>
            </div>
            <form action={signOut}>
              <button
                type="submit"
                className="text-sm font-body text-white/55 hover:text-white/85 transition-colors"
              >
                Sign out
              </button>
            </form>
            {profile?.username && (
              <div className="border-t border-white/[0.08] pt-4">
                <DeleteAccountSection username={profile.username} />
              </div>
            )}
          </div>
```

- [ ] **Step 6: Verificar**

Run: `npm run dev` → `/dashboard/settings`.
- "Download JSON" descarga `ingegno-export.json` con profile, projects, updates, github_activity (y SIN access_token).
- "Delete account" abre el confirm. Escribir un username incorrecto → error "does not match".
- **Probar el borrado real con una cuenta de prueba desechable:** escribir el username correcto → "Permanently delete" → redirige a `/` y la sesión termina. Verificar en Supabase → Authentication que el usuario ya no existe y que su fila en `profiles` desapareció (cascade).

Run: `npx tsc --noEmit` → sin errores.

- [ ] **Step 7: Commit**

```bash
git add lib/supabase/admin.ts app/api/export/route.ts app/actions/account.ts components/dashboard/DeleteAccountSection.tsx app/dashboard/settings/page.tsx
git commit -m "feat(account): add data export (JSON) and account deletion with confirmation"
```

---

## Verificación final (tras completar todas las tareas)

- [ ] **Step 1: Tests**

Run: `npm test`
Expected: PASS — incluye `lib/plan.test.ts` y `lib/links.test.ts`.

- [ ] **Step 2: Tipos y lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: sin errores de tipos; lint limpio (los `<img>` llevan su `eslint-disable`).

- [ ] **Step 3: Build de producción**

Run: `npm run build`
Expected: build OK.

- [ ] **Step 4: Smoke test en móvil real (375px)**

- Editar perfil desde el móvil: **añadir roles funciona** (botón "+ Add", sin saltar de campo).
- Añadir un link y un contact email, guardar, recargar → persisten.
- Subir portada a un proyecto desde el móvil.
- Settings: descargar export, abrir el confirm de borrado (sin confirmarlo).
- Perfil público: hero con botón "Get in touch" + iconos de links; portadas en los proyectos.

- [ ] **Step 5: Actualizar CLAUDE.md**

Marcar en `CLAUDE.md` (sección "Completado") las features S0 entregadas y mover "Analytics de visitas" etc. al backlog según corresponda. Commit:

```bash
git add CLAUDE.md
git commit -m "docs: mark S0 features as shipped"
```

---

## Notas de seguridad (revisar antes de mergear)

- `links` y `contact_email` se sanitizan: las URLs pasan por `normalizeUrl` + `isValidHttpUrl` (solo http/https, bloquea `javascript:`). El render usa `target="_blank" rel="noopener noreferrer"`.
- El export NUNCA incluye `github_connections.access_token`.
- El borrado usa el service role **solo server-side** (`lib/supabase/admin.ts`), nunca en el bundle del cliente, y exige confirmación por username.
- El bucket `project-images` tiene política de INSERT restringida a la carpeta del propio `auth.uid()`.
```